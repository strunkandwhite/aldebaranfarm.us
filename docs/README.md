# docs/

Project documentation.

- **`architecture.md`** — how the file-based data layer is isolated and how the
  future direct-booking + Airbnb/VRBO calendar sync plugs into `lib/booking`.
- **`style-guide.md`** — the shipped brand tokens (colors, fonts, links, radius)
  and where each one lives.
- **`project-plan.md`** — the original build plan: site map, page specs,
  content model, and build phases. Historical reference; where it disagrees
  with the code or the other docs, the code wins.

# public/images/brand/

Brand assets — **placeholders to be added later** with the style guide:

- `logo.svg` — site wordmark/logo (used in `components/layout/Header`).
- `favicon.ico` — currently lives at `app/favicon.ico` (Next default).
- `og-image.png` — social share image referenced from `app/layout.tsx` metadata.

Drop the real files here and wire them up when branding is finalized. See
`docs/style-guide.md`.
